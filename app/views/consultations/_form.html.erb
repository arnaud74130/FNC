<%= bootstrap_nested_form_for(@consultation) do |f| %>
  <%= f.text_field :code %>
  <%= f.date_field :debut %>
  <%= f.date_field :fin %>
  <%= f.collection_check_boxes(:etablissement_ids, @etablissements, :id, :nom) %>

  <%= f.fields_for :markets %>
  <%= f.link_to_add "Ajout d'un marché", :markets %>
  <br/>
  <%= f.submit %>
<% end %>
